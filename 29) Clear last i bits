#include <bits/stdc++.h>
using namespace std;

int main()       // Can be solved by the process of clearing Bits in range where l=i,r=1
{
    int n=23; // 10111
    
    int i=3;
    
    // clear last i bits
    
    n=(n>>i);
   
   cout<<(n<<i)<<endl;  // 10000  means 16
    
    return 0;
}
